import { expect } from 'chai'
import 'mocha'

describe('Observable', () => {

  it('should not break references')
  it('should handle objects')
  it('should only emit if the value has changed')
  it('should handle arrays')
  it('should keep methods on objects')
  it('should emit when adding props')
  it('should emit when removing props')
  it('should emit when changing props')

})